<div class="col-auto">
    <div class="header__social-wrapper">
    <a href="{{ $href ?? null }}" class="{{ $linkClass ?? null }}">
       {{ $name ?? null }} <img src="{{ $src ?? null }}"
                           alt="{{ $alt ?? null }}"
                           class="img-fluid {{ $iconClass ?? null }}">
    </a>
    </div>
</div>